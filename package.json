{
  "name": "gretel",
  "version": "4.0.0",
  "license": "CC-BY-NC-SA-4.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/UUDigitalHumanitieslab/gretel.git"
  },
  "scripts": {
    "build": "npm run build:api && npm run build:web-ui",
    "build:api": "cd api && composer install",
    "build:web-ui": "cd web-ui && npm install && npm run build"
  }
}